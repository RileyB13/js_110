/*
What is the return value of the following statement? Why?
*/

['ant', 'bear', 'caterpillar'].pop().length;

/*
pop removes and returns the last element in the caller array. So pop method returns
'caterpillar', and then we call length on that string which returns 11.
*/
